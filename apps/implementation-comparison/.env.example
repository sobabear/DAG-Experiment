# Local environment for implementation-comparison.
# Copy to .env if useful. Never commit .env or real credentials.
# OPENAI_API_KEY is read by the OpenAI SDK from the process environment.
# Leave these blank in the committed example. Do not paste a key here.

IMPL_COMPARISON_LLM_PROVIDER=
IMPL_COMPARISON_LLM_MODEL=
IMPL_COMPARISON_LLM_ENDPOINT=
OPENAI_API_KEY=
